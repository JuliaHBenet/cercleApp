<h1>Usuaris disponibles</h1>
<ul>
  <% @users.each do |user| %>
  <li>
    <h2>
      Usuari #<%= link_to user.id, user_path(user)%>
    </h2>
    <p>Nom: <%= user.name %></p>
    <p>Email: <%= user.email %></p>
    <p>Rol: <%= user.role %></p>
  </li>
  <% end %>

</ul>
