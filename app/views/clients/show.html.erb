<h2><%= @client.name %></h2>
<p>#<%= @client.id %> </p>
<p>Nom de contacte: <%= @client.contactname %></p>
<p>Email: <%= @client.contactemail %></p>
<p>Telèfon: <%= @client.contactphone1 %></p>
<p>NIF: <%= @client.idcard %></p>
<p>Adreça: <%= @client.address %> , <%= @client.postcode %>, <%= @client.city %></p>
<% if @client.comments %>
  <p>Comentaris: <%= @client.comments %></p>
<% end %>
<% if policy(@client).edit? %>
<%= link_to "Edit this client", edit_client_path(@client), class: "btn btn-primary btn-wd" %>
<% end %>

<!-- COMMENTED OUT
<% if policy(@client).destroy? %>
<%= link_to "Destroy this client", clients_path(@client) %>
<% end %>
 END OF COMMENTED OUT -->

<%= link_to "Back to all clients", clients_path, class: "btn btn-primary btn-wd" %>

 <h3>Llista de reserves</h3>
<% if policy(Event.new).create? %>
  <%= link_to "Make a new event", new_client_event_path({client_id: @client}), class: "btn btn-success btn-wd" %>
<% end %>
<ul>
  <% @client.events.order(eventstart: :desc).where(client: @client).each do |event| %>
  <li>
    <h3>
      #<%= event.id %> <%= link_to event.name, event_path(event)%>
    </h3>
    <p>Data inici: <%= event.eventstart.strftime("%d %b %Y a les %H:%M")  %></p>
    <p>Data fi: <%= event.eventend.strftime("%d %b %Y a les %H:%M") %></p>
    <p>Sala: <%= event.room.name %></p>
    <p>Client: <%= event.client.name %></p>

  </li>
  <% end %>
</ul>



